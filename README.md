# 🎓 Advanced SQL for Data Engineering - Teaching Materials

## ภาพรวม

ชุดเอกสารการสอน **Advanced SQL** สำหรับนักศึกษา Computer Science ปี 2-3 ใช้ข้อมูล IBM HR Analytics เพื่อวิเคราะห์การลาออกของพนักงาน (Employee Attrition)

**ระยะเวลาการสอน**: 6 ชั่วโมง (เช้า 3 ชั่วโมง + บ่าย 3 ชั่วโมง)

### 📊 แหล่งที่มาของข้อมูล
ข้อมูล `HR.csv` ที่ใช้ในโปรเจกต์นี้มาจาก:
- **Dataset**: IBM HR Analytics - Predictive Employee Attrition
- **แหล่งที่มา**: [Kaggle Dataset](https://www.kaggle.com/datasets/nicolaszalazar73/ibm-hr-analytics-predictive-employee-attrition/data)
- **ผู้เผยแพร่**: Nicolas Zalazar
- **License**: ใช้เพื่อการศึกษาเท่านั้น

---

## 📁 ไฟล์ในโปรเจค

### สำหรับนักศึกษา:
| ไฟล์ | คำอธิบาย |
|------|----------|
| `hr_database_setup.sql` | SQL script สร้าง database ทั้งหมด (DROP, CREATE, INSERT, INDEX, VIEWS) |
| `sql_exercises.sql` | แบบฝึกหัดและการบ้าน พร้อมตัวอย่างและเฉลย |

### สำหรับอาจารย์:
| ไฟล์ | คำอธิบาย |
|------|----------|
| `teaching_guide.md` | คู่มือการสอนแบบละเอียด พร้อมแผนการสอน, tips, และ checklist |
| `process_hr_data.py` | Python script แปลง HR.csv เป็น normalized tables |

### ข้อมูล (CSV):
| ไฟล์ | แถว | คำอธิบาย |
|------|-----|----------|
| `HR.csv` | 1,470 | ข้อมูลดิบจาก IBM |
| `departments.csv` | 3 | ข้อมูลแผนก |
| `job_roles.csv` | 31 | ข้อมูลตำแหน่งงาน |
| `employees.csv` | 1,470 | ข้อมูลพนักงาน |
| `employee_compensation.csv` | 1,470 | ข้อมูลค่าตอบแทน |
| `employee_satisfaction.csv` | 1,470 | ข้อมูลความพึงพอใจ |
| `employee_work_history.csv` | 1,470 | ข้อมูลประวัติการทำงาน |

---

## 🚀 Quick Start สำหรับนักศึกษา

### 1. ติดตั้ง PostgreSQL
```bash
# Download จาก: https://www.postgresql.org/download/
# ติดตั้งพร้อม PgAdmin4
```

### 2. สร้าง Database
```sql
-- เปิด psql หรือ PgAdmin4
CREATE DATABASE hr_analytics;
\c hr_analytics

-- หรือใน PgAdmin4: คลิกขวา > Create > Database > ใส่ชื่อ "hr_analytics"
```

### 3. Import Data
```sql
-- วิธีที่ 1: ใน psql
\i hr_database_setup.sql

-- วิธีที่ 2: ใน PgAdmin4
-- คลิกขวาที่ database > Query Tool > เปิดไฟล์ hr_database_setup.sql > Execute
```

### 4. ทดสอบ
```sql
-- ตรวจสอบว่า import สำเร็จ
SELECT 'Database setup completed!' as status;

-- ดูข้อมูล
SELECT * FROM vw_employee_full_info LIMIT 10;
SELECT * FROM vw_attrition_by_department;
```

### 5. เริ่มทำแบบฝึกหัด
เปิดไฟล์ `sql_exercises.sql` และทำตามตัวอย่างและแบบฝึกหัด

---

## 📊 Database Schema

### ER Diagram
```
┌─────────────────┐
│  departments    │
│  - dept_id (PK) │──┐
│  - dept_name    │  │
└─────────────────┘  │
                     │
┌─────────────────┐  │     ┌──────────────────────────┐
│   job_roles     │  │     │       employees          │
│ - job_role_id(PK│──┼────→│ - employee_id (PK)       │
│ - job_role_name │  │     │ - age, gender            │
│ - job_level     │  │     │ - marital_status         │
│ - department_id │←─┘     │ - education              │
└─────────────────┘        │ - education_field        │
                           │ - distance_from_home     │
                           │ - department_id (FK)     │
                           │ - job_role_id (FK)       │
                           └──────────────────────────┘
                                     │
                ┌────────────────────┼────────────────────┐
                │                    │                    │
┌───────────────▼──────┐  ┌──────────▼─────────┐  ┌──────▼───────────────┐
│employee_compensation │  │employee_satisfaction│  │employee_work_history │
│ - compensation_id    │  │ - satisfaction_id   │  │ - work_history_id    │
│ - employee_id (FK)   │  │ - employee_id (FK)  │  │ - employee_id (FK)   │
│ - monthly_income     │  │ - job_satisfaction  │  │ - years_at_company   │
│ - percent_salary_hike│  │ - env_satisfaction  │  │ - over_time          │
│ - stock_option_level │  │ - work_life_balance │  │ - attrition (Yes/No) │
└──────────────────────┘  └─────────────────────┘  └──────────────────────┘
```

**Normalization Level**: Third Normal Form (3NF)

### 📋 รายละเอียดตาราง

#### 1. **departments** - ตารางแผนก
เก็บข้อมูลแผนกต่างๆ ในองค์กร

| Column | Type | Constraint | คำอธิบาย |
|--------|------|------------|----------|
| department_id | SERIAL | PRIMARY KEY | รหัสแผนก (Auto-increment) เช่น 1, 2, 3 |
| department_name | VARCHAR(100) | NOT NULL, UNIQUE | ชื่อแผนก เช่น "Research & Development", "Sales", "Human Resources" |

**จำนวนแผนก**: 3 แผนก

---

#### 2. **job_roles** - ตารางตำแหน่งงาน
เก็บข้อมูลตำแหน่งงานและระดับงาน โดยแต่ละตำแหน่งสังกัดแผนกใดแผนกหนึ่ง

| Column | Type | Constraint | คำอธิบาย |
|--------|------|------------|----------|
| job_role_id | SERIAL | PRIMARY KEY | รหัสตำแหน่งงาน (Auto-increment) |
| job_role_name | VARCHAR(100) | NOT NULL | ชื่อตำแหน่ง เช่น "Sales Executive", "Research Scientist", "Manager" |
| job_level | INTEGER | NOT NULL, CHECK (1-5) | ระดับตำแหน่ง: 1=Entry, 2=Junior, 3=Senior, 4=Lead, 5=Executive |
| department_id | INTEGER | FOREIGN KEY | รหัสแผนกที่ตำแหน่งนี้สังกัด (อ้างอิง departments.department_id) |

**จำนวนตำแหน่ง**: 31 ตำแหน่ง (เช่น Sales Representative, Laboratory Technician, Healthcare Representative)

---

#### 3. **employees** - ตารางพนักงาน
เก็บข้อมูลพื้นฐานและข้อมูลส่วนตัวของพนักงาน

| Column | Type | Constraint | คำอธิบาย |
|--------|------|------------|----------|
| employee_id | INTEGER | PRIMARY KEY | รหัสพนักงาน (Employee Number จากระบบ) เช่น 1, 2, 5, 7... |
| age | INTEGER | NOT NULL, CHECK (18-100) | อายุพนักงาน (ปี) เช่น 25, 34, 45 |
| gender | VARCHAR(10) | NOT NULL | เพศ: "Male" หรือ "Female" |
| marital_status | VARCHAR(20) | NOT NULL | สถานภาพสมรส: "Single", "Married", "Divorced" |
| education | INTEGER | NOT NULL, CHECK (1-5) | ระดับการศึกษา: 1=ต่ำกว่าปริญญาตรี, 2=ปริญญาตรี, 3=ปริญญาโท, 4=ปริญญาเอก, 5=อื่นๆ |
| education_field | VARCHAR(50) | NOT NULL | สาขาที่จบการศึกษา: "Life Sciences", "Medical", "Marketing", "Technical Degree", "Other", "Human Resources" |
| distance_from_home | INTEGER | NOT NULL | ระยะทางจากบ้านถึงที่ทำงาน (กิโลเมตร) เช่น 1, 5, 10, 29 |
| department_id | INTEGER | FOREIGN KEY | รหัสแผนกที่สังกัด (อ้างอิง departments.department_id) |
| job_role_id | INTEGER | FOREIGN KEY | รหัสตำแหน่งงาน (อ้างอิง job_roles.job_role_id) |

**จำนวนพนักงาน**: 1,470 คน

---

#### 4. **employee_compensation** - ตารางค่าตอบแทน
เก็บข้อมูลเงินเดือน โบนัส และสวัสดิการของพนักงาน

| Column | Type | Constraint | คำอธิบาย |
|--------|------|------------|----------|
| compensation_id | SERIAL | PRIMARY KEY | รหัสข้อมูลค่าตอบแทน (Auto-increment) |
| employee_id | INTEGER | FOREIGN KEY | รหัสพนักงาน (อ้างอิง employees.employee_id) |
| monthly_income | INTEGER | NOT NULL | เงินเดือนรายเดือน (หน่วยเงิน) เช่น 2,500, 5,000, 15,000 |
| monthly_rate | INTEGER | NOT NULL | อัตราค่าจ้างรายเดือน (อาจแตกต่างจาก monthly_income) |
| daily_rate | INTEGER | NOT NULL | อัตราค่าจ้างรายวัน เช่น 100-1,500 |
| hourly_rate | INTEGER | NOT NULL | อัตราค่าจ้างรายชั่วโมง เช่น 30-100 |
| percent_salary_hike | INTEGER | NOT NULL | เปอร์เซ็นต์การขึ้นเงินเดือนล่าสุด เช่น 11%, 15%, 25% |
| stock_option_level | INTEGER | NOT NULL, CHECK (0-3) | ระดับ Stock Option: 0=ไม่มี, 1=น้อย, 2=ปานกลาง, 3=สูง |
| standard_hours | INTEGER | NOT NULL, DEFAULT 80 | จำนวนชั่วโมงทำงานมาตรฐานต่อเดือน (โดยทั่วไป 80 ชม.) |

**ใช้สำหรับ**: วิเคราะห์โครงสร้างเงินเดือน, ความเท่าเทียม, และความสัมพันธ์ระหว่างเงินเดือนกับการลาออก

---

#### 5. **employee_satisfaction** - ตารางความพึงพอใจ
เก็บข้อมูลความพึงพอใจและประสิทธิภาพการทำงานของพนักงาน

| Column | Type | Constraint | คำอธิบาย |
|--------|------|------------|----------|
| satisfaction_id | SERIAL | PRIMARY KEY | รหัสข้อมูลความพึงพอใจ (Auto-increment) |
| employee_id | INTEGER | FOREIGN KEY | รหัสพนักงาน (อ้างอิง employees.employee_id) |
| environment_satisfaction | INTEGER | NOT NULL, CHECK (1-4) | ความพึงพอใจต่อสภาพแวดล้อมการทำงาน: 1=ต่ำมาก, 2=ต่ำ, 3=ปานกลาง, 4=สูง |
| job_satisfaction | INTEGER | NOT NULL, CHECK (1-4) | ความพึงพอใจต่องานที่ทำ: 1=ต่ำมาก, 2=ต่ำ, 3=ปานกลาง, 4=สูง |
| relationship_satisfaction | INTEGER | NOT NULL, CHECK (1-4) | ความพึงพอใจต่อความสัมพันธ์กับเพื่อนร่วมงาน: 1-4 |
| work_life_balance | INTEGER | NOT NULL, CHECK (1-4) | ความสมดุลระหว่างชีวิตกับการทำงาน: 1=แย่มาก, 2=แย่, 3=ดี, 4=ดีมาก |
| job_involvement | INTEGER | NOT NULL, CHECK (1-4) | ระดับความมุ่งมั่นต่องาน: 1=ต่ำมาก, 2=ต่ำ, 3=ปานกลาง, 4=สูง |
| performance_rating | INTEGER | NOT NULL, CHECK (1-4) | คะแนนประเมินผลการทำงาน: 1=ต่ำ, 2=ปานกลาง, 3=ดี, 4=ดีเยี่ยม |

**ใช้สำหรับ**: วิเคราะห์ความสัมพันธ์ระหว่างความพึงพอใจกับการลาออก, หาจุดที่ต้องปรับปรุง

---

#### 6. **employee_work_history** - ตารางประวัติการทำงาน
เก็บข้อมูลประวัติการทำงาน อายุงาน และสถานะการลาออก

| Column | Type | Constraint | คำอธิบาย |
|--------|------|------------|----------|
| work_history_id | SERIAL | PRIMARY KEY | รหัสข้อมูลประวัติ (Auto-increment) |
| employee_id | INTEGER | FOREIGN KEY | รหัสพนักงาน (อ้างอิง employees.employee_id) |
| total_working_years | INTEGER | NOT NULL | อายุงานรวมทั้งหมด (ปี) รวมที่ทำงานบริษัทอื่นมาก่อน เช่น 0-40 ปี |
| years_at_company | INTEGER | NOT NULL | อายุงานในบริษัทนี้ (ปี) เช่น 0-40 ปี |
| years_in_current_role | INTEGER | NOT NULL | อายุในตำแหน่งปัจจุบัน (ปี) เช่น 0-18 ปี |
| years_since_last_promotion | INTEGER | NOT NULL | จำนวนปีนับจากการเลื่อนตำแหน่งครั้งล่าสุด เช่น 0-15 ปี |
| years_with_curr_manager | INTEGER | NOT NULL | อายุการทำงานกับผู้จัดการคนปัจจุบัน (ปี) เช่น 0-17 ปี |
| num_companies_worked | INTEGER | NOT NULL | จำนวนบริษัทที่เคยทำงานมาก่อน (ไม่นับบริษัทปัจจุบัน) เช่น 0-9 บริษัท |
| training_times_last_year | INTEGER | NOT NULL | จำนวนครั้งที่เข้ารับการอบรมในปีที่แล้ว เช่น 0-6 ครั้ง |
| business_travel | VARCHAR(30) | NOT NULL | ความถี่ในการเดินทางเพื่อธุรกิจ: "Non-Travel", "Travel_Rarely", "Travel_Frequently" |
| over_time | VARCHAR(3) | NOT NULL, CHECK | การทำงานล่วงเวลา: "Yes" = ทำ, "No" = ไม่ทำ |
| attrition | VARCHAR(3) | NOT NULL, CHECK | **สถานะการลาออก**: "Yes" = ลาออกแล้ว (237 คน), "No" = ยังทำงานอยู่ (1,233 คน) |

**ตัวแปรสำคัญ**: `attrition` คือตัวแปรเป้าหมาย (target variable) สำหรับการวิเคราะห์และทำนายการลาออก

**ใช้สำหรับ**: วิเคราะห์ความสัมพันธ์ระหว่างอายุงาน, overtime, การเดินทาง กับการลาออก

**Normalization Level**: Third Normal Form (3NF)

---

## 📚 หัวข้อที่สอน

### ส่วนที่ 1: SQL เบื้องต้น (1 ชม.)
- ✅ CRUD Operations
- ✅ Aggregate Functions
- ✅ GROUP BY, HAVING

### ส่วนที่ 2: JOINs และ Subqueries (1 ชม.)
- ✅ INNER JOIN, LEFT JOIN, RIGHT JOIN
- ✅ Multiple Table JOINs (3+ tables)
- ✅ Self JOIN
- ✅ Subqueries (WHERE, FROM, SELECT)
- ✅ Correlated Subqueries
- ✅ EXISTS, IN

### ส่วนที่ 3: Common Table Expressions (1 ชม.)
- ✅ WITH Clause (CTE)
- ✅ Multiple CTEs
- ✅ Recursive CTEs
- ✅ CTE vs Subquery

### ส่วนที่ 4-5: Window Functions (2 ชม.)
- ✅ ROW_NUMBER(), RANK(), DENSE_RANK()
- ✅ LAG(), LEAD()
- ✅ FIRST_VALUE(), LAST_VALUE()
- ✅ NTILE()
- ✅ Aggregate Window Functions (SUM, AVG OVER)
- ✅ PARTITION BY
- ✅ Frame Clauses (ROWS BETWEEN, RANGE BETWEEN)

### ส่วนที่ 6: Advanced Techniques (1 ชม.)
- ✅ CASE WHEN
- ✅ COALESCE, NULLIF
- ✅ UNION, UNION ALL
- ✅ Performance Tips (INDEX, EXPLAIN)

---

## 🎯 แบบฝึกหัด

### แบบฝึกหัดในคลาส (มีเฉลย)
1. **Department Attrition Analysis** - Multi-table JOIN & Aggregation
2. **Top 3 Employees by Department** - CTE & Window Functions  
3. **Attrition Factors Analysis** - Multiple CTEs & Complex Queries
4. **Salary Movement Analysis** - Window Functions with LAG/LEAD

### การบ้าน (4 ข้อ)
1. **Employee Risk Scoring** - CASE WHEN และ Conditional Logic
2. **Department Performance Dashboard** - Comprehensive Analytics
3. **Salary Cohort Analysis** - NTILE และ Trend Analysis
4. **Predictive Features Ranking** - Feature Importance Analysis

---

## 💡 Insights จากข้อมูล

### สถิติทั่วไป:
- **จำนวนพนักงาน**: 1,470 คน
- **อัตราการลาออก**: 16.1% (237 คน)
- **แผนก**: 3 แผนก (Research & Development, Sales, Human Resources)
- **ตำแหน่งงาน**: 31 ตำแหน่ง

### ปัจจัยสำคัญที่ส่งผลต่อการลาออก:
1. 🔴 **Overtime**: พนักงานที่ทำ OT มีอัตราลาออก 30%+
2. 🔴 **อายุงานต่ำ**: 60% ของคนลาออกอยู่ในช่วง 0-5 ปีแรก
3. 🔴 **Job Satisfaction**: คะแนนความพึงพอใจต่ำ (1-2)
4. 🔴 **เงินเดือน**: คนลาออกมีเงินเดือนเฉลี่ยต่ำกว่า
5. 🔴 **ระยะทางไกล**: Distance from home มาก

---

## 🛠️ เครื่องมือที่ใช้

| เครื่องมือ | เวอร์ชัน | วัตถุประสงค์ |
|-----------|---------|-------------|
| PostgreSQL | 14+ | Database Server |
| PgAdmin 4 | Latest | GUI Tool สำหรับจัดการ PostgreSQL |
| Python | 3.8+ | ประมวลผลข้อมูล (สำหรับอาจารย์) |
| Pandas | Latest | Data manipulation (สำหรับอาจารย์) |

---

## 📖 วิธีใช้สำหรับอาจารย์

### การเตรียมตัวก่อนสอน:
1. อ่าน `teaching_guide.md` ทั้งหมด
2. ทดสอบ run `hr_database_setup.sql`
3. ทดลองทำทุกแบบฝึกหัดใน `sql_exercises.sql`
4. เตรียม slides/notes เพิ่มเติมถ้าต้องการ

### ในคลาส:
1. แจก `hr_database_setup.sql` และ `sql_exercises.sql`
2. ให้นักศึกษา setup database ก่อน (15 นาที)
3. สอนตาม schedule ใน `teaching_guide.md`
4. ใช้ live coding และให้นักศึกษาลองทำตาม
5. ตรวจสอบความเข้าใจเป็นระยะ

### หลังคลาส:
1. มอบการบ้าน 4 ข้อ (อยู่ใน `sql_exercises.sql`)
2. กำหนดส่งภายใน 1 สัปดาห์
3. เปิดช่องทางให้ปรึกษาได้

---

## 🔄 วิธีสร้าง Database ใหม่ (สำหรับอาจารย์)

ถ้าต้องการแก้ไขข้อมูลหรือสร้าง database ใหม่:

```bash
# 1. แก้ไข HR.csv
# 2. รัน Python script
python process_hr_data.py

# Output:
# - departments.csv
# - job_roles.csv  
# - employees.csv
# - employee_compensation.csv
# - employee_satisfaction.csv
# - employee_work_history.csv
# - hr_database_setup.sql (regenerated)
```

---

## 📚 แหล่งเรียนรู้เพิ่มเติม

### สำหรับนักศึกษา:
- [PostgreSQL Tutorial](https://www.postgresqltutorial.com/)
- [Mode Analytics SQL Tutorial](https://mode.com/sql-tutorial/)
- [SQLZoo Interactive](https://sqlzoo.net/)
- [LeetCode Database Problems](https://leetcode.com/problemset/database/)

### สำหรับอาจารย์:
- [PostgreSQL Docs - Window Functions](https://www.postgresql.org/docs/current/tutorial-window.html)
- [Use The Index, Luke](https://use-the-index-luke.com/) - Performance
- [Modern SQL](https://modern-sql.com/) - Best Practices

---

## 🐛 Troubleshooting

### ปัญหาที่พบบ่อย:

**Q: Import ไฟล์ SQL แล้วเกิด error**
```
A: ตรวจสอบว่าสร้าง database "hr_analytics" แล้ว
   และ connect ไปที่ database นั้นก่อน import
```

**Q: Query ช้า**
```
A: 1. ตรวจสอบว่า INDEX ถูกสร้างหรือยัง (มีอยู่ในไฟล์ SQL)
   2. ใช้ EXPLAIN ANALYZE เพื่อดู query plan
   3. ลด LIMIT ในขณะทดสอบ
```

**Q: ไม่เห็น Tables**
```
A: ตรวจสอบว่า import สำเร็จ และ refresh ใน PgAdmin4
   คลิกขวาที่ "Tables" > Refresh
```

**Q: Permission denied**
```
A: ตรวจสอบ PostgreSQL user permissions
   อาจต้อง run เป็น superuser หรือแก้ไข permissions
```

---

## 🤝 การสนับสนุน

หากมีคำถามหรือพบปัญหา:
- ปรึกษาอาจารย์ผู้สอน
- อ่าน `teaching_guide.md` เพิ่มเติม
- ดูตัวอย่างและเฉลยใน `sql_exercises.sql`

---

## 📝 License & Credits

- **ข้อมูล**: IBM HR Analytics Employee Attrition & Performance Dataset
- **สร้างโดย**: Python automation script
- **วัตถุประสงค์**: การศึกษาเท่านั้น

---

## ✨ สรุป

โปรเจคนี้ประกอบด้วย:
- ✅ Database ที่ออกแบบดี (3NF) พร้อม 6 tables
- ✅ ข้อมูล 1,470 พนักงานจริง พร้อม insights
- ✅ แบบฝึกหัด 4 ข้อ + การบ้าน 4 ข้อ
- ✅ เฉลยทุกข้อพร้อมคำอธิบาย
- ✅ คู่มือการสอนละเอียด
- ✅ ครอบคลุม Advanced SQL ทุกหัวข้อสำคัญ

**พร้อมใช้งานทันที โดยไม่ต้องเตรียมข้อมูลเพิ่มเติม! 🎉**

---

**Happy Teaching & Learning! 📚✨**
